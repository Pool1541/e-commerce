const availableFilters = {
  SUB_CATEGORY: 'subCategory',
  BRAND: 'brand',
};

export const ITEMS_PER_PAGE = 20;
export const MIN_DISCOUNT = 50;
export const FILTERS = [availableFilters.SUB_CATEGORY, availableFilters.BRAND];
